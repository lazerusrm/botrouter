# Security

Do not report credentials in a public issue. Report vulnerabilities privately through GitHub's security-advisory flow for this repository.

BotRouter must never package, print, copy, or commit provider tokens. Use provider-owned login flows or Grok Bot's masked `secret-request` UI. Auto-review remains the authority for consequential external actions; reliability fixes must not bypass a native denial.
